CREATE TABLE topic (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    path VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    subscribe BOOLEAN DEFAULT TRUE
);
CREATE TABLE data (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    id_topic BIGINT NOT NULL,
    value VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (id_topic) REFERENCES topic(id) ON DELETE CASCADE
);

ALTER TABLE topic ADD COLUMN latest_data VARCHAR(255) NULL;


//chua can
CREATE TABLE arena (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

ALTER TABLE topic ADD CONSTRAINT fk_topic_arena FOREIGN KEY (id_arena) REFERENCES arena(id) ON DELETE CASCADE;